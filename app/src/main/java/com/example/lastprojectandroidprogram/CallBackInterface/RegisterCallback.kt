package com.example.lastprojectandroidprogram.CallBackInterface

import com.example.lastprojectandroidprogram.Response.ResponseRegister

interface RegisterCallback {
    fun onResult(response: ResponseRegister?)
}